queue_rules:
  - name: default
    merge_conditions:
      - base=main

pull_request_rules:
  - name: automatic merge
    conditions:
      - base=main
    actions:
      queue:
        name: default
